import {
  CARDINALDIRS 
} from './rootsConstants';
import {
  isStringSimilar
} from './rootsUtilities';

export const parseInput = raw => {
  let rawsplit = raw.split(' ');
  switch(rawsplit.length) {
    case 0:
      return ['?'];
    case 1:
      return ['?'];
    case 2:
      return ['?'];
    default:
      return ['?'];
  }
};